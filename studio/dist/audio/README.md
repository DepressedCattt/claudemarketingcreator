# Audio Files

Drop your audio files here (MP3, WAV, AAC, OGG).

They will be available in the AudioEditor as `/audio/<filename>`.

## Example
Drop `beat.mp3` here → reference as `/audio/beat.mp3` in the editor.
